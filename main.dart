import 'package:assignment/screens/ui.dart';
import 'package:flutter/material.dart';
void main() => runApp (Ui());

class Ui extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return MaterialApp(
      home: AUi(),
    );

  }

}